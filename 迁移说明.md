# 换电脑 / 迁移说明

> 目标：让 AIVA 不绑定在任何一台电脑上。

---

## 一、先说清楚：哪些东西跟电脑无关，哪些绑着电脑

| 内容 | 存在哪 | 换电脑能带走吗 |
|---|---|---|
| **代码** | `llm-companion/` 目录 | ✅ 完全可带走（就是那个 zip） |
| **3D 模型** | `assets/models/*.glb` + `*.jpg`（1.8MB） | ✅ 已打进 zip，跟代码一起走 |
| **App 打包配置** | `app.json` / `metro.config.js` / `eas.json` | ✅ 跟代码走 |
| **聊天记录 / 亲密值 / 心情 / 记忆** | 手机本地 AsyncStorage | ❌ **在手机上，不在电脑上** —— 换电脑不影响，换手机才丢 |
| **接入哪家大模型 + API Key** | 手机本地 AsyncStorage | ❌ 同上，且在手机上填，电脑上永远看不到 |
| **`node_modules/`** | 电脑本地 | ❌ 故意不打进 zip，新电脑 `npm install` 一条命令重建 |

**关键结论：你的数据其实不在电脑上，在手机上。**
电脑只是"编译工厂"。所以换电脑真正要做的，只是把代码搬过去 + 重装依赖。

---

## 二、快速迁移（3 条命令）

把 `aiva-app.zip` 拷到新电脑（U盘 / 网盘 / 微信传给自己都行），然后：

```bash
# 0. 新电脑需要先装 Node.js 20+  （https://nodejs.org 下载 LTS 版即可）

# 1. 解压
unzip aiva-app.zip && cd llm-companion

# 2. 装依赖（会自动装 1000+ 个包，第一次约 2-5 分钟）
npm install

# 3. 跑起来看效果
npx expo start
```

然后：
- 手机装 **Expo Go**（App Store / 各安卓应用商店都有），扫终端里的二维码 → 手机上就能跑
- 或者终端按 `w` → 浏览器里跑

**这一步做完，App 就已经在新电脑上活过来了。** 3D 模型、聊天、摸头互动全都在。

---

## 三、想在云端打开、不依赖任何电脑的代码

> ⚠️ 要先跟你说明一个技术事实，避免误会：
> **Expo / React Native 项目无法直接"上传到云端打开"。**
> 它不是网页，是一个原生 App 工程——必须经过编译。
> 所以"云端"对这类项目有两种真实可行的含义，我都能给你做：

### 方案 A：代码放云端仓库（推荐，最省事）

代码进 GitHub 后，任何电脑 `git clone` 就能继续开发，不再需要"传 zip"。

```bash
cd llm-companion
git init
git add .
git commit -m "AIVA: AI 伴侣 App，含真 3D 模型"
# 在 github.com 新建一个空仓库，然后：
git remote add origin https://github.com/<你的用户名>/aiva-companion.git
git push -u origin main
```

需要我帮你做的话，我可以：
- 生成 `.gitignore`（已经准备好了，见仓库根目录）
- 帮你把 git 仓库初始化好、提交好第一次

### 方案 B：真正"云端打开"——把 App 打到云上构建

这才是手机 App 的正解。用 **EAS Build**（Expo 官方云构建）：

```bash
npm install -g eas-cli
eas login                       # 注册/登录 expo.dev（免费）
eas build --platform android    # 云上编译出 .apk
eas build --platform ios        # 云上编译出 .ipa（需 Apple 开发者账号 $99/年）
```

编译在 Expo 的服务器上跑，**不需要你本地装 Android SDK / Xcode**。
编完给一个下载链接，手机上直接装。

这一步能真正解决"这台电脑装不了 Android SDK"的问题。

### 方案 C：云端 IDE

- **Expo Snack**（snack.expo.dev）—— 官方在线编辑器，适合试代码片段，但装不下我们的 3D 模型资源
- **GitHub Codespaces** —— 完整的云上 VS Code，能在浏览器里跑 `npm install` / `expo start`

---

## 四、当前这台电脑上"不能做"的两件事（以及原因）

我之前排查过，如实告诉你：

| 想做的 | 状态 | 原因 |
|---|---|---|
| 本地编译 Android APK | ❌ 不行 | 没装 JDK、Android SDK、Gradle |
| 本地编译 iOS | ❌ 物理上不可能 | Windows 无法运行 Xcode，iOS 只能 macOS 或云端构建 |

**但用方案 B 的 EAS 云构建，这两个都能绕过。** 你只需要一个 Expo 账号。

---

## 五、换手机的话，数据怎么办

App 里的亲密值 / 聊天记录 / 记忆存在手机本地。要跟着人走，有两个办法：

1. **导出导入**（现在就能做）：设置页有"导出存档 / 导入存档"，复制一段文本即可迁移
2. **换成云同步**（要改代码）：需要接后端账号系统

如果你想做第 2 种（真正的跨设备同步），我可以给 App 加一个轻量后端：
- 用户注册/登录
- 存档同步到云端
- 任意设备登录就恢复

这是"数据也不绑设备"的唯一正解。**要做的话跟我说，我来加。**

---

## 六、从零重建需要的东西（清单）

新电脑上，按顺序：

```
1. Node.js 20+          https://nodejs.org
2. Git（可选）           https://git-scm.com
3. 项目代码              从 zip 解压，或 git clone
4. npm install          重建 node_modules
5. 手机装 Expo Go        App Store / 应用商店搜 "Expo Go"
6. npx expo start
```

就这些。3D 模型文件已在仓库里，不需要重新生成。

---

## 七、如果你要重新生成 3D 模型

模型已经在了，一般不用。万一要改：

```bash
# 需要 WorkBuddy 的 buddy-cloud 工具链，脚本已备好：
python tools/fetch_3d.py <job_id> <名字>.glb    # 下载生成的模型
python tools/split_texture.py <名字>            # 拆成 模型 + 独立贴图（体积缩 19 倍）
node   tools/validate_model.mjs                 # 验证模型能否正常解析
node   tools/pipeline.test.mjs                  # 端到端测试整条 3D 管线
node   tools/mkzip.mjs                          # 重新打包
```

> **为什么要 `split_texture.py`：** 生成出来的 glb 里贴图占 10MB+，
> 而且 React Native 没有 DOM，three 自带的贴图加载器在手机上会直接崩。
> 拆开后模型 500KB、贴图 110KB，由 App 自己解码 —— 体积小 19 倍且真机可跑。

---

## 八、需要我继续做的

- [ ] `git init` + 首次提交（方案 A）
- [ ] 配好 EAS 云构建（`eas.json` + 命令），让你一条命令出 APK
- [ ] 加云端账号系统，实现**聊天记录跨设备同步**
- [ ] App 图标 + 启动页设计（ImageGen 之前返回空，需要重试）

---

## 九、没有脸的人形机器人（Unitree G1 这类）怎么办

**结论：不需要脸。** 这是当初做设备能力抽象时留出的余地。

### 为什么说不需要

`src/avatar/companionBundle.js` 输出的每一帧长这样：

```js
{
  t: 0.5, face: { A:{jawOpen:0.3}, B:{jawOpen:0.32}, C:{jawOpen:0.28}, D:{jawOpen:0.2} },
  head: { pitch: 2.1, yaw: -4.0, roll: 0.2 },
  gaze: { pitch: 0, yaw: -0.15 },
  flags: { speaking: true, sleep: false, emotionChanged: true, bargeIn: false },
  emotion: { name:'warm', valence:0.55, arousal:0.35, intensity:0.7, mode:'stable' }
}
```

里面的 `face` 字段只负责**脸**。机器人没脸，这一项就是空的，系统照样能跑。

真正驱动情绪表达的是另外两样东西：

1. `emotion` —— 情绪是什么、多强烈、正向还是负向。这个字段**跟脸完全无关**。
2. `DeviceProfile` —— 描述"这台硬件到底有什么器官"，决定把 `emotion` 翻译成什么动作。

### 同一帧，按硬件能力翻译成不同东西

| 机器有什么 | 怎么表达情绪 | 是否需要 SDK |
|---|---|---|
| 扬声器（一定有） | 改语音韵律：语速、音高、能量、停顿——这部分现在就能用 | 否 |
| 头部 / 身体自由度 | 姿态幅度：valence 高 → 抬头挺胸；arousal 高 → 动作幅度变大 | 是 |
| LED 灯带 / 点阵屏 | 颜色 + 呼吸节律 + 闪烁模式 | 是 |
| 胸口 / 头部小屏 | 退回渲染一张程式化脸（绘制代码已经有了） | 否 |
| 以上全没有 | 只有声音——照样是一个能聊天的伙伴 | 否 |

**关键在于：最后一行“什么都没有、只剩声音”其实已经是个完整产品了。** 因为说话的节奏直接来自 TTS 音频的能量包络，比任何手工编排的动画都准；情绪也融在语音韵律里，而不是只挂在嘴角的弧度上。

### 落地需要的条件

要接 LED 或自由度，我至少需要下面其中一样：

- 型号 + 固件版本（例如 "G1 EDU，固件 x.y"），我去找对应 SDK 的控制接口
- 或者你手上的 SDK 文档 / 示例代码

这两个我现在都没有，所以**我没有办法负责任地声称已经支持**。这是唯一还没做的部分，也是最后一个 TODO。

### 参考入口

- classifier 对外只输出 `warm / playful / calm / sad / excited / tender` 六个备选方案
- transport-layer：TTS + Inversion

> 注：速率节拍取决于目标机器人的控制频率（常见 10–500 Hz），这块要等拿到 SDK 才能定，现在先不敢写死。
